import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const planos = await prisma.mSPLANOPAGAMENTO.findMany(); console.log('Planos:', planos.map(p => ({ id: p.CODPLPAG, tipo: p.tipo_pagamento }))); const lastOrder = await prisma.mspedido.findFirst({ orderBy: { numpedido: 'desc' }, include: { pagamentos_multiplos: true, mscontas_receber: true, movimentos_caixa: true } }); console.log(JSON.stringify(lastOrder, null, 2)); } main().catch(console.error).finally(() => prisma.\());
